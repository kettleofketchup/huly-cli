# Docker Compose recipes
# Call as: just compose::up, just compose::down

# Start services with Docker Compose
up:
    @echo "No compose configuration defined yet"
    @echo "Add docker-compose.yml and customize this recipe"

# Stop services
down:
    @echo "No compose configuration defined yet"
